<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="clertonleal.com.weather">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".WeatherApplication"
        android:allowBackup="true"
        android:icon="@mipmap/weather_icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/weather_icon"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".view.activity.MainActivity"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".view.activity.SelectCitiesActivity"
            android:label="@string/cities"
            android:screenOrientation="portrait" />
        <activity
            android:name=".view.activity.SelectWeatherActivity"
            android:label="@string/weather"
            android:screenOrientation="portrait" />
        <activity
            android:name=".view.activity.ResultActivity"
            android:label="@string/results"
            android:screenOrientation="portrait" />
    </application>

</manifest>